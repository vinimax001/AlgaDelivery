spring:
  application:
    name: delivery-tracking
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5433/deliverydb_test
    username: postgres
    password: postgres